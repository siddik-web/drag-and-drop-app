import { ThemeType } from "../types/types";

// New file for theme constants
export const initialTheme: ThemeType = {
    backgroundColor: "white",
    borderColor: "black",
    name: "default",
    primaryColor: "black",
    secondaryColor: "white",
    fontFamily: "Arial",
}; 